pub struct Centeroid {
    points: Vec<u8>,
    count: usize,
}

pub struct Centeroids {
    splits: Vec<Centeroid>,
}

impl Centeroids {
    pub fn new(dims: usize, num_centeroids: usize) -> Self {
        todo!()
    }
}
